{
  "config": {
    "step": {
      "user": {
        "title": "Webhook Conversation",
        "description": "Configure webhook conversation integration",
        "data": {
          "name": "Name",
          "webhook_url": "Webhook URL",
          "ai_task_webhook_url": "AI Task Webhook URL",
          "output_field": "Output field name",
          "prompt": "System Prompt",
          "timeout": "Request timeout (seconds)",
          "enable_streaming": "Enable response streaming",
          "auth_type": "Authentication type"
        }
      },
      "auth": {
        "title": "Authentication Configuration",
        "description": "Configure authentication for webhook requests",
        "data": {
          "username": "Username",
          "password": "Password"
        }
      }
    },
    "error": {
      "invalid_webhook_url": "Invalid webhook URL",
      "invalid_auth": "Invalid authentication credentials",
      "unknown": "Unexpected error occurred"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Configure Webhook Conversation",
        "description": "Configure options for webhook conversation",
        "data": {
          "name": "Name",
          "webhook_url": "Webhook URL",
          "ai_task_webhook_url": "AI Task Webhook URL",
          "output_field": "Output field name",
          "prompt": "System Prompt",
          "timeout": "Request timeout (seconds)",
          "enable_streaming": "Enable response streaming",
          "auth_type": "Authentication type"
        }
      },
      "auth": {
        "title": "Authentication Configuration",
        "description": "Configure authentication for webhook requests",
        "data": {
          "username": "Username",
          "password": "Password"
        }
      }
    }
  },
  "selector": {
    "auth_type": {
      "options": {
        "none": "No authentication",
        "basic_auth": "Basic HTTP authentication"
      }
    }
  }
}
